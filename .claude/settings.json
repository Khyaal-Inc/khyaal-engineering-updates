{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; echo \"$f\" | grep -q '\\.js$' && node --check \"$f\" && echo \"SYNTAX OK: $f\"; } 2>/dev/null || true",
            "timeout": 10,
            "statusMessage": "Checking JS syntax..."
          }
        ]
      }
    ]
  }
}
